/**
 * Theme functions file.
 *
 * global value tc
 * - retina ( is retina device )
 * - mobile ( is mobile device )
 *
 */

( function( $ ) {
	console.log("is retina: "+tc.retina);
	console.log("is mobile: "+tc.mobile);
} )( jQuery );